spring.application.name=java-akademika

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/mydatabase
spring.datasource.username=myuser
spring.datasource.password=secret
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Security Keys (gere as suas próprias chaves)
# openssl genrsa -out keypair.pem 2048
# openssl rsa -in keypair.pem -pubout -out public.pem
# openssl pkcs8 -topk8 -inform PEM -outform PEM -in keypair.pem -out private.pem -nocrypt
security.keys.private=
security.keys.public=
